version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install uvicorn
run:
  runtime-version: 3.11
  command: uvicorn GeminiPaperCreator:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
